# KarenAI — application dependencies
streamlit>=1.32.0
requests>=2.31.0
python-dotenv>=1.0.0

# Step 2 — RAG / Context Store (local vector database)
chromadb>=0.5.0
langchain>=0.3.0

# KarenAI — Step 2: RAG / Context Store (local ChromaDB + LangChain)
chromadb>=0.5.0
langchain>=0.3.0
langchain-chroma>=0.2.0
langchain-core>=0.3.0
